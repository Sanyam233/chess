import Modal from "../UI/Modal/Modal";

const PawnPromotionAlert = () => {

  return (
    <Modal>
      <h2>Pawn Promotion</h2>
      <p>Pawn promotion under way. Your turn will resume once the opponent is done selecting their promoted piece</p>
    </Modal>
  );
};

export default PawnPromotionAlert;
